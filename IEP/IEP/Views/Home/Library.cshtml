@model IEnumerable<IEP.BusinessLogic.Entities.LiteratureItem>

@if (!Model.Any())
{
    <p class="chapter-text-content">Литература ещё не добавлена</p>
}
@foreach (var item in Model)
{
    <a href="@item.Path">@item.Title</a>
}